class Event {
  
  constructor(type, id, amount, fromAge, toAge, rate, extra) {
    this.type = type;
    this.id = id;
    this.amount = amount;
    this.fromAge = fromAge;
    this.toAge = toAge;
    this.rate = rate;
    this.extra = extra;
  }
  
}

